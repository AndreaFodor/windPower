name: wind_power
channels:
  - conda-forge
dependencies:
  - jupyter 
  - matplotlib >= 3.8
  - openpyxl >= 3.0
  - pandas >= 2.0
  - pip >= 24.0
  - pyarrow 
  - py-xgboost
  - python = 3.12
  - scikit-learn >= 1.4
  - sqlalchemy
  - xgboost = 2.1.4
  - pmdarima >= 2.0
  - interpret
  - statsforecast = 2.0.1
  - pip:
      - cinemagoer
      - lxml
      - pynytimes
      - pyjokes == 0.6.0
      - plotly == 5.24.0
      - seaborn == 0.13.2
